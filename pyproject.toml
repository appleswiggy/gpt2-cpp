[project]
name = "gpt2-cpp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
    "transformers>=5.3.0",
]
